git_url("git@github.com:kumpeapps/fastlane_certs.git")

storage_mode("git")

type("developer_id") # For macOS distribution outside App Store

app_identifier(["com.managednebula.client"])
keychain_name("login.keychain-db")
team_id("2T42Z3DM34")
include_mac_in_profiles(true)
username("jakumpe@justinkumpe.net") # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
